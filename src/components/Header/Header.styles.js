import { StyleSheet } from "react-native";

export default StyleSheet.create({
    header: {
        flexDirection: 'row',
        margin: 5,
        justifyContent: 'space-between',
      },
      header_text: {
        fontSize: 30,
        padding: 10,
        fontWeight: '800',
        color: '#FFA500',
      },
});